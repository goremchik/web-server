version: "3.3"
services:
    nodeserver:
        build:
            context: ./
        ports:
            - "3000:3000"
    nginx:
        restart: always
        build:
            context: ./
            dockerfile: ./nginx/Dockerfile
        ports:
            - "8080:80"
